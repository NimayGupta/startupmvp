# Optional. A form provider endpoint that accepts a POST (e.g. Formspree:
# https://formspree.io/f/<id>). When set at build time, the #waitlist band
# renders the email-capture form; when unset, it falls back to a mailto CTA.
# PUBLIC_ vars are embedded in the static build — never put secrets here.
PUBLIC_FORM_ENDPOINT=
